# 2026-08-01 — PIXELES Progress Log

## Schema Ampliado
- Rewrote `scripts/init-db.sql` with extended schema:
  - New tables: `branches`, `users`, `clients`, `sales`, `sale_items`, `products`, `reservations`, `audit_logs`
  - Extended existing `consoles` with `branch_id`, `type` (console/pc), `image_url`
  - Extended `sessions` with `branch_id`, `client_id`, `pause_started_at`, `paused_seconds`
  - Extended status enums: consoles adds 'off', 'no_internet', 'error'; sessions adds 'paused'
  - Extended `settings` with `branch_id`
  - Seed data: 8 products, 12 games, 1 branch, price config (Q currency)
- Rewrote `backend/src/config/initDb.js`: now uses single `client.query(sql)` in simple mode (no params) to support multi-statement SQL; made non-lethal (logs error but doesn't abort startup)
- Pushed and verified: schema applied successfully on Render, health OK, /api/consoles OK

## Auth Planned
- Dependencies installed: jsonwebtoken, bcryptjs
- Next: authMiddleware, authController, login/register routes
- Admin default: admin / admin123

## Next Steps (pending)
- Create auth middleware + controller + routes
- Extend timer service (pause/resume/add time)
- New controllers: products, clients, branches, sales
- Frontend redesign: dark glassmorphism, gaming dashboard, login page
# 2026-08-01 — PIXELES Progress Log

## Schema Ampliado
- Rewrote `scripts/init-db.sql` with extended schema:
  - New tables: `branches`, `users`, `clients`, `sales`, `sale_items`, `products`, `reservations`, `audit_logs`
  - Extended existing `consoles` with `branch_id`, `type` (console/pc), `image_url`
  - Extended `sessions` with `branch_id`, `client_id`, `pause_started_at`, `paused_seconds`
  - Extended status enums: consoles adds 'off', 'no_internet', 'error'; sessions adds 'paused'
  - Extended `settings` with `branch_id`
  - Seed data: 8 products, 12 games, 1 branch, price config (Q currency)
- Rewrote `backend/src/config/initDb.js`: now uses single `client.query(sql)` in simple mode (no params) to support multi-statement SQL; made non-lethal (logs error but doesn't abort startup)
- Pushed and verified: schema applied successfully on Render, health OK, /api/consoles OK

## Auth System (DONE ✅)
- Created `backend/src/middleware/authMiddleware.js` — JWT verification + role-based access control
- Created `backend/src/controllers/authController.js` — login, register (admin-only via middleware), me
- Created `backend/src/routes/auth.js` — POST /login, POST /register, GET /me
- Updated `backend/src/config/initDb.js` with `seedAdmin()` — creates admin/admin123 superadmin
- Updated `backend/src/index.js` to mount /api/auth routes and call seedAdmin()
- Committed + pushed to GitHub (commit: auth system)

## Business Modules (DONE ✅)
- Created `backend/src/controllers/productController.js` — full CRUD for products/inventory
- Created `backend/src/routes/products.js` — admin/superadmin protected routes
- Created `backend/src/controllers/branchController.js` — full CRUD for branches
- Created `backend/src/routes/branches.js` — auth protected routes
- Updated `backend/src/index.js` to mount /api/products and /api/branches
- Committed + pushed to GitHub (commit: business modules)

## Timer & Console Extensions (DONE ✅)
- Created `backend/src/controllers/consoleExtended.js` — pauseSession, resumeSession, addTime, setStatus
- Completely rewrote `backend/src/services/timerService.js` — supports pause, resume, add time, progressive alerts (10/5/1 min), auto-end
- Updated `backend/src/routes/consoles.js` with new routes: /pause, /resume, /add-time, /status
- Committed locally but NOT YET PUSHED — GitHub token expired

## 🔴 Critical Bug Found & Fixed
`consoleExtended.js` was missing both `module.exports` and all `require` statements (pool, socketService, timerService). This caused the server to crash on startup with "callback is not a function" → Render returned 502.
- **Fix applied**: Added proper requires at top + `module.exports = { pauseSession, resumeSession, addTime, setStatus }`
- **Status**: Fix is committed locally but **cannot push** — GitHub token expired (ghp_oN…sFyd no longer valid)

## ⚠️ BLOCKED: GitHub Token
Need Antonio to provide a new GitHub Personal Access Token (classic, scope: `repo`) to push the consoleExtended fix. Without this push, Render is serving 502 — the old deployed code crashes on startup.

## Next Steps (after push)
1. Push consoleExtended fix → Render auto-deploys → verify all endpoints work
2. Frontend redesign: dark glassmorphism, gaming dashboard, login page
3. Client controller + routes
4. Sales controller + routes
5. Reservations controller
