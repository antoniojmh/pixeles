# 2026-08-02 — PIXELES Status (00:02 UTC)

## Current State
- All backend code is pushed to GitHub (`antoniojmh/pixeles`) and deployed on Render
- `consoleExtended.js` fix (missing exports/imports) was committed (`caa67e3`) and pushed successfully
- Render Web Service: `pixeles-api` at `https://pixeles-api.onrender.com`

## Active Issues
- **Render Free Tier Cold Start**: Service sleeps after inactivity. Cold start takes ~60-90s, during which all routes return 502.
- **Intermittent 502**: Even after health check passes, some API routes return empty/502 during warm-up. `initDb()` + `seedAdmin()` run on startup; if DB connection is slow, routes may not be ready.
- **Cloudflare Cache**: `pixeles-api.onrender.com` goes through Cloudflare (`cf-ray` header). Old cached responses could show stale HTML instead of JSON.

## What's Working (when server is warm)
- `/api/health` — returns JSON with uptime
- `/api/consoles` — GET returns JSON (verified)
- Auth routes, product routes, branch routes — registered in index.js, should work when warm

## TODO (Antonio's project)
- Frontend redesign: dark glassmorphism, gaming dashboard, login page
- Client controller + routes
- Sales controller + routes
- Reservations controller
- Redis integration (Upstash, currently non-blocking)
